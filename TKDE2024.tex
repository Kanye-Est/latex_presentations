\documentclass[aspectratio=169]{beamer} % 使用 16:9 宽屏

% 引入必要的宏包
\usepackage[UTF8]{ctex}
\usepackage{amsmath}
\usepackage{amssymb} 
\usepackage{bm}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{tcolorbox}
\usepackage{tikz} 
\usepackage{pifont} 

% 主题与颜色设置
\usetheme{Madrid} 
\usecolortheme{default}
\setbeamertemplate{navigation symbols}{} 
\setbeamertemplate{footline}[frame number] 

% 自定义高亮颜色
\definecolor{myblue}{RGB}{33, 113, 181}
\definecolor{myred}{RGB}{203, 24, 29}
\definecolor{mygreen}{RGB}{44, 160, 44}

\title[Distribution Leakage in Cross-Silo FL]{On Data Distribution Leakage in Cross-Silo Federated Learning}
\subtitle{2024 TKDE —— 跨孤岛联邦学习中的分布推断攻击}
\author{文献分享}
\date{\today}

\begin{document}

\begin{frame}
    \titlepage
\end{frame}

\begin{frame}{Agenda}
    \tableofcontents[hideallsubsections]
\end{frame}

% ==========================================
\section{1. 前置知识 (Preliminaries)}
% ==========================================

\begin{frame}{1.1 跨孤岛联邦学习 (Cross-Silo FL)}
    \begin{itemize}
        \item \textbf{定义：} 多个机构（如银行、医院）在不交换原始数据的情况下，协作训练一个共享模型。
        \item \textbf{交互流程：} 
        \begin{enumerate}
            \item \textbf{本地训练：} 客户端利用本地数据计算梯度 $\nabla L(x, y; \theta)$。
            \item \textbf{参数同步：} 客户端上传模型更新（梯度或权重差）至中心服务器。
            \item \textbf{全局聚合：} 服务器执行聚合算法（如 FedAvg）更新全局模型。
        \end{enumerate}
        \item \textbf{核心特征：} 参与方数量少（Silo）、计算能力强、数据量大。
    \end{itemize}
\end{frame}

\begin{frame}{1.2 差分隐私 (Differential Privacy) 在 FL 中的应用}
    为了防止梯度泄露个体隐私，通常引入 \textbf{DP-SGD} 防御机制：
    
    \begin{tcolorbox}[colback=blue!5,colframe=myblue,title=DP-SGD 核心步骤]
        \begin{enumerate}
            \item \textbf{裁剪 (Clipping)：} 限制每个梯度向量的 $\ell_2$ 范数，防止单个极端样本主导更新。
            \item \textbf{加噪 (Noise Addition)：} 在聚合梯度中注入高斯噪音 $n \sim \mathcal{N}(0, \sigma^2 C^2)$。
        \end{enumerate}
    \end{tcolorbox}
    
    \textbf{隐私承诺：} 攻击者无法以显著概率判断训练集中是否包含“张三”这一条特定记录。
\end{frame}

% ==========================================
\section{2. 问题陈述与威胁模型 (Problem Statement)}
% ==========================================

\begin{frame}{2.1 什么是分布泄露？ (Problem Definition)}
    \begin{columns}
        \begin{column}{0.45\textwidth}
            \textbf{攻击者的目标：} \\
            推断出参与方 $k$ 的\textbf{全局统计分布}，包括：
            \begin{itemize}
                \item \textbf{特征分布 $P(X)$：} 数据在各特征维度上的均值、方差。
                \item \textbf{标签分布 $P(Y)$：} 训练集中正负样本的真实比例（极具商业价值）。
            \end{itemize}
            \vspace{0.2cm}
            \textbf{为什么是盲区？} \\
            DP 保护了个体，但为了保证模型可用性，它必须保留数据的统计特性。
        \end{column}
        \begin{column}{0.55\textwidth}
            \begin{center}
                \includegraphics[width=\textwidth]{TKDE2024pic/fig_fl_threat.png}
            \end{center}
        \end{column}
    \end{columns}
\end{frame}

\begin{frame}{2.2 诚实但好奇的服务器 (Threat Model)}
    \begin{itemize}
        \item \textbf{服务器能力：} 
        \begin{itemize}
            \item 观察每一轮客户端上传的梯度更新。
            \item 知道当前的全局模型参数 $\theta_t$。
            \item \textbf{无先验知识：} 攻击者没有参与方的辅助数据（无影子数据）。
        \end{itemize}
        \item \textbf{防御假设：} 客户端可能开启了裁剪和加噪形式的差分隐私防御。
    \end{itemize}
\end{frame}

% ==========================================
\section{3. 核心攻击方法论 (Attack Methodology)}
% ==========================================

\begin{frame}{3.1 攻击预处理：本地模型参数聚合 (Algorithm 1)}
    \begin{columns}
        \begin{column}{0.45\textwidth}
            \textbf{算法背景：} 服务器需要从连续的训练轮次中提炼出一个能代表客户端分布的本地参数 $\theta_k$。
            
            \vspace{0.2cm}
            \textbf{逻辑解析：}
            \begin{itemize}
                \item \textbf{行 1-2：} 计算每一轮本地参数 $\theta_k^{(t)}$ 与最终全局模型 $\theta_G$ 的欧氏距离 $C_k^{(t)}$。
                \item \textbf{行 3-4：} 利用 \textbf{Softmax} 对这些距离进行加权处理，距离越远权重越大（捕捉显著差异）。
                \item \textbf{行 5：} 得到聚合后的本地模型 $\theta_k$，作为后续攻击的输入。
            \end{itemize}
        \end{column}
        \begin{column}{0.55\textwidth}
            \begin{center}
                \includegraphics[width=\textwidth]{TKDE2024pic/alg1_agg.png}
            \end{center}
        \end{column}
    \end{columns}
\end{frame}

\begin{frame}{3.2 方案 A：针对可微模型的 PGD 攻击 (Algorithm 2)}
    \begin{columns}
        \begin{column}{0.45\textwidth}
            \textbf{Projected Gradient Descent Attack (PGDA)：}
            \begin{enumerate}
                \item \textbf{初始化 (行 3)：} 随机采样起始分布 $x_0$。
                \item \textbf{梯度匹配 (行 5-7)：} 
                \begin{itemize}
                    \item 计算 Loss 对输入数据的梯度。
                    \item 通过投影梯度下降更新虚拟分布，使其产生的梯度逼近截获的真实梯度。
                \end{itemize}
                \item \textbf{抗噪扰动 (行 8-13)：} 引入高斯扰动并进行一致性校验，筛选出真正鲁棒的合成样本，从而重构整体分布。
            \end{enumerate}
        \end{column}
        \begin{column}{0.55\textwidth}
            \begin{center}
                \includegraphics[width=\textwidth]{TKDE2024pic/alg2_pgda.png}
            \end{center}
        \end{column}
    \end{columns}
\end{frame}

\begin{frame}{3.3 方案 B：针对不可微模型的通用攻击 (1/2)}
    \begin{columns}
        \begin{column}{0.45\textwidth}
            \textbf{算法 3：外层合成循环}
            \begin{itemize}
                \item \textbf{目标：} 合成满足分布特征的数据集 $S$。
                \item \textbf{自适应阈值 (行 9-11)：} 如果在一个阈值下合成失败次数过多，算法会动态放松阈值 $\tau_{min}$，扩大搜索容忍度。
                \item \textbf{逻辑：} 这是一个不断迭代的过程，直至合成出足够数量的有效样本。
            \end{itemize}
        \end{column}
        \begin{column}{0.55\textwidth}
            \begin{center}
                \includegraphics[width=\textwidth, trim=0 300 0 0, clip]{TKDE2024pic/alg3_generic.png}
            \end{center}
        \end{column}
    \end{columns}
\end{frame}

\begin{frame}{3.4 方案 B：针对不可微模型的通用攻击 (2/2)}
    \begin{columns}
        \begin{column}{0.45\textwidth}
            \textbf{算法 3：核心 Search 函数}
            \begin{itemize}
                \item \textbf{启发式搜索：} 在高维特征空间中随机采样并寻找能减小 Loss 的更优分布解。
                \item \textbf{维度缩减 (行 29-30)：} 如果连续多次被拒绝，算法会认为搜索空间过大，从而\textbf{减半特征维度} $d$，通过缩小搜索半径来强迫收敛。
            \end{itemize}
        \end{column}
        \begin{column}{0.55\textwidth}
            \begin{center}
                \includegraphics[width=\textwidth, trim=0 0 0 350, clip]{TKDE2024pic/alg3_generic.png}
            \end{center}
        \end{column}
    \end{columns}
\end{frame}

\begin{frame}{3.5 PGDA 数学本质：基于梯度匹配的优化}
    攻击者求解以下优化问题来逼近真实分布：
    \begin{equation}
        \min_{\mathbf{D}_{dummy}} \mathcal{L}_{match} = \left\| \nabla W(\mathbf{D}_{dummy}, \theta) - \nabla W_{intercepted} \right\|^2 + \alpha \cdot \mathcal{R}(\mathbf{D}_{dummy})
    \end{equation}
    
    \begin{itemize}
        \item $\nabla W_{intercepted}$：中心服务器截获的真实梯度更新。
        \item $\mathbf{D}_{dummy}$：攻击者构造的虚拟数据分布参数。
        \item $\mathcal{R}(\cdot)$：正则化项，用于确保生成分布的合理性。
    \end{itemize}
\end{frame}

% ==========================================
\section{4. 实验评估与防御分析 (Evaluation \& Defense)}
% ==========================================

\begin{frame}{4.1 击穿最强防御：为什么 DP 失效了？}
    \begin{columns}
        \begin{column}{0.5\textwidth}
            \textbf{实验观察：}
            \begin{itemize}
                \item \textbf{个体的干扰：} 噪音很大时，单个样本的特征被完全模糊。
                \item \textbf{总体的抗噪性：} “分布”是成千上万个样本的平均统计特性。
                \item \textbf{结论：} 零均值的高斯噪音在聚合过程中会相互抵消，宏观分布特征对 DP 噪音具有天然的鲁棒性。
            \end{itemize}
        \end{column}
        \begin{column}{0.5\textwidth}
            \begin{center}
                \begin{tikzpicture}
                    \draw[thick, dashed, fill=yellow!10, rounded corners] (0,0) rectangle (6, 4.0);
                    \node at (3, 2.5) {\Large \textbf{[请在此处插入]}};
                    \node at (3, 1.5) {\Large \textbf{[TKDE 2024 结果图]}};
                \end{tikzpicture}
            \end{center}
        \end{column}
    \end{columns}
\end{frame}

% ==========================================
\section{5. 总结与启示 (Conclusion)}
% ==========================================

\begin{frame}{5.1 研究启示}
    \begin{tcolorbox}[colback=blue!5,colframe=myblue,title=核心结论 (Takeaways)]
        \begin{itemize}
            \item \textbf{隐私保护的错位：} 跨孤岛场景下，“宏观分布”往往比“微观记录”更具商业敏感性，但现有防御却无能为力。
            \item \textbf{攻击普适性：} 无论模型是否可微，服务器均能通过截获的更新信息高精度还原数据分布。
        \end{itemize}
    \end{tcolorbox}
\end{frame}

\begin{frame}
    \begin{center}
        \Huge \textbf{Q \& A} \\
        \vspace{1cm}
        \Large 感谢聆听！
    \end{center}
\end{frame}

\end{document}